#!/bin/bash

week=$(date +%V)
year=$(date +%Y)
updates=/Users/$(whoami)/Dropbox/.whatidid/updates/$year
file=$updates/$week.md

while read line; do
  created=$(echo $line | cut -d: -f1)
  message=$(echo $line | cut -d: -f2)
  echo $(date -r $created +'%A')':' $message
done < $file

